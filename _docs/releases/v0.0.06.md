**v0.0.6** - Login, Register + Authentication (JWT, password hashing, requireAuth, requireRole)
 
Goals accomplished
- [X] Register: create user with hashed password
- [X] Login: verify, issue access token (JWT)
- [X] Context: parse Authorization: Bearer <token> → ctx.user
- [X] Guarded resolvers: only allow certain roles